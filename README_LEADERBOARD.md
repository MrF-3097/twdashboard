# TowerImob Leaderboard UI

- Source: Apps Script Web App at `NEXT_PUBLIC_LEADERBOARD_URL`.
- Quests are NOT hardcoded. The UI derives boolean columns automatically. Columns with `(GROUP)` are rendered after the individual block.
- Auto-refresh every 10s without page reload.
- Use ro-RO locale for timestamps.

